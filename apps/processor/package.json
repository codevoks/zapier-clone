{
  "name": "processor",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch --env-file ../../.env src/index.ts",
    "build": "tsc -b",
    "test": "echo \"No app-level tests: worker logic is covered by @repo/processor and @repo/validation unit tests\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/client": "^7.10.0",
    "@repo/db": "workspace:*",
    "@repo/logger": "workspace:*",
    "@repo/processor": "workspace:*",
    "@repo/validation": "workspace:*",
    "kafkajs": "^2.2.4",
    "tsx": "^4.19.0"
  }
}
